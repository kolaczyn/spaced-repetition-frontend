import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spaced-repetition-system-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
